# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  WayBlog — 环境变量配置模板
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  复制此文件为 .env 并填入真实值

# ━━━ 数据库 ━━━
DATABASE_URL="postgresql://wayblog:wayblog_dev_password@localhost:6432/wayblog"

# ━━━ Redis ━━━
# 本项目 compose 默认把 Redis 映射到宿主机 6381，避免和其他项目 Redis 冲突
REDIS_URL="redis://localhost:6381/0"
REDIS_KEY_PREFIX="wayblog"

# ━━━ NextAuth 认证 ━━━
# 本地开发端口；如被系统占用，可临时改为其他端口
PORT=3610

# 开发环境
NEXTAUTH_URL="http://localhost:3610"
# 内网穿透/模拟部署时改为实际访问地址
# NEXTAUTH_URL="https://wayblog-demo.example.com"

# 生成方式：openssl rand -base64 32
NEXTAUTH_SECRET="wayblog-local-demo-secret-replace-before-deploy"

# ━━━ 站点地址 ━━━
# 仅保留会随部署环境变化的公开地址；品牌名和文案改由 src/config/site.ts 维护
SITE_URL="http://localhost:3610"
# 内网穿透/模拟部署时改为实际访问地址
# SITE_URL="https://wayblog-demo.example.com"

# ━━━ 管理员账号（仅用于 seed 脚本初始化）━━━
ADMIN_EMAIL="admin@wayblog.local"
ADMIN_PASSWORD="way-local-demo-password"
ADMIN_NAME="Way"

# ━━━ 图片上传配置 ━━━
UPLOAD_MAX_SIZE=5242880   # 5MB in bytes
UPLOAD_DIR="public/uploads"
# 允许的文件类型（逗号分隔）
UPLOAD_ALLOWED_TYPES="image/jpeg,image/png,image/gif,image/webp"

# ━━━ Rate Limiting ━━━
RATE_LIMIT_LOGIN=5        # 登录接口：5次/分钟/IP
RATE_LIMIT_VIEWS=1        # 浏览量记录：1次/秒/IP/文章
RATE_LIMIT_SEARCH=30      # 搜索接口：30次/分钟/IP
RATE_LIMIT_API=60         # 其他API：60次/分钟/IP

# ━━━ AI 模型配置 ━━━
# 通用超时时间（毫秒）
AI_TIMEOUT_MS=120000

# 阿里百炼（推荐）
AI_PROVIDER="aliyun-bailian"
# 填入真实百炼 API Key；不要提交真实密钥
DASHSCOPE_API_KEY="your-dashscope-api-key"
DASHSCOPE_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
DASHSCOPE_MODEL="qwen3.6-plus"

# Ollama（本地模型）
# AI_PROVIDER="ollama"
# OLLAMA_BASE_URL="http://127.0.0.1:11434"
# OLLAMA_MODEL="qwen2.5:1.5b"

# ━━━ 可选：第三方服务（迭代阶段启用）━━━
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="notice@wayblog.local"
# SMTP_PASSWORD="wayblog-demo-smtp-password"

# STORAGE_PROVIDER="local"  # local | minio | oss
# MINIO_ENDPOINT="http://localhost:9000"
# MINIO_ACCESS_KEY="minioadmin"
# MINIO_SECRET_KEY="minioadmin"
# MINIO_BUCKET="wayblog"

