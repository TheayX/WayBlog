# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  WayBlog — 环境变量配置模板
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  复制此文件为 .env 并填入真实值

# ━━━ 数据库 ━━━
DATABASE_URL="postgresql://wayblog:your_db_password@localhost:6432/wayblog"

# ━━━ NextAuth 认证 ━━━
# 开发环境
NEXTAUTH_URL="http://localhost:3333"
# 生产环境（部署时修改）
# NEXTAUTH_URL="https://yourdomain.com"

# 生成方式：openssl rand -base64 32
NEXTAUTH_SECRET="your-random-secret-key-here-replace-this-at-least-32-chars"

# ━━━ 站点信息 ━━━
SITE_NAME="Way"
SITE_DESCRIPTION="A Journey of Code and Thought"
SITE_URL="http://localhost:3333"
# 生产环境（部署时修改）
# SITE_URL="https://yourdomain.com"

# ━━━ 管理员账号（仅用于 seed 脚本初始化）━━━
ADMIN_EMAIL="way20031208@gmail.com"
ADMIN_PASSWORD="change-this-to-strong-password"
ADMIN_NAME="Way"

# ━━━ 图片上传配置 ━━━
UPLOAD_MAX_SIZE=5242880   # 5MB in bytes
UPLOAD_DIR="public/uploads"
# 允许的文件类型（逗号分隔）
UPLOAD_ALLOWED_TYPES="image/jpeg,image/png,image/gif,image/webp"

# ━━━ Rate Limiting ━━━
RATE_LIMIT_LOGIN=5        # 登录接口：5次/分钟/IP
RATE_LIMIT_VIEWS=1        # 浏览量记录：1次/秒/IP/文章
RATE_LIMIT_SEARCH=30      # 搜索接口：30次/分钟/IP
RATE_LIMIT_API=60         # 其他API：60次/分钟/IP

# ━━━ AI 模型配置 ━━━
# 通用超时时间（毫秒）
AI_TIMEOUT_MS=60000

# 阿里百炼（推荐）
AI_PROVIDER="aliyun-bailian"
DASHSCOPE_API_KEY="your-dashscope-api-key"
DASHSCOPE_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
DASHSCOPE_MODEL="qwen3.6-plus"

# Ollama（本地模型）
# AI_PROVIDER="ollama"
# OLLAMA_BASE_URL="http://127.0.0.1:11434"
# OLLAMA_MODEL="qwen2.5:1.5b"

# ━━━ 可选：第三方服务（迭代阶段启用）━━━
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"

# STORAGE_PROVIDER="local"  # local | minio | oss
# MINIO_ENDPOINT="http://localhost:9000"
# MINIO_ACCESS_KEY="minioadmin"
# MINIO_SECRET_KEY="minioadmin"
# MINIO_BUCKET="wayblog"

